#Recomendacíon para una mejor Experiencia:
#instalar Python desde la pagina Oficial de https://www.python.org/downloads/
#Iniciar "Powershell" y ejecutar como administrador
#Una vez que habra la ventana de powershell ejecuta los siguientes comandos:
#python -m pip install pg8000
#python -m pip install pg8000 pyinstaller
#python -m pip install psycopg2-binary
